#blue
version: '3'
services:
    nextjs: 
        build:
            context: /home/ec2-user/next_tutorial
            dockerfile: ./deploy/Dockerfile
        restart: unless-stopped
        ports:
            - 3001:3000
    nginx:
        build: ./nginx
        ports:
            - 80:80