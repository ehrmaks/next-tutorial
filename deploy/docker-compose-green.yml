#green
version: '3'
services:
    nextjs: 
        build:
            context: /home/ec2-user/next_tutorial
            dockerfile: ./deploy/Dockerfile
        restart: unless-stopped
        ports:
            - 3002:3000
    nginx:
        build: ./nginx
        ports:
            - 80:80